# CargoShipmentTracker
Abstract Shipment + Standard/Express/SameDay, Insurable interface, enum state machine, custom exception, CargoCompany (ArrayList+HashMap), Comparator strategies, menu-driven Main. Matches spec exactly (per-km rates, weight caps, % insurance, I/O). Includes academic report + UML.
